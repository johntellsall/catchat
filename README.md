# CatChat!

# Firebase

## TODO
*  https://github.com/firebase/quickstart-python

# Run

    docker-compose build
    docker-compose up

# Develop

    docker-compose build
    docker-compose run web bash
    # inside container:
    pytest
